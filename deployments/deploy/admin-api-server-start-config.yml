apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-api-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      admin-api: 1
    toolBinaries:
      - check-component
      - attribute-to-credential
    maxFileDescriptors: 10000
