apiVersion: v1
kind: ConfigMap
metadata:
  name: bot-rpc-server-start-config
data:
  discovery.yml: |
    serviceBinaries:
      bot-rpc: 1
    toolBinaries:
      - check-component
      - attribute-to-credential
    maxFileDescriptors: 10000
